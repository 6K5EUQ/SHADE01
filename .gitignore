__pycache__/
*.pyc
*.ulg
.venv/

# QGC app data and build trees (large, machine-local)
QGroundControl/
qgc-build/
qgc-src/
qgc-5.1.4/

# QGC binaries and flight_review (large, machine-local)
Applications/

# Python virtualenv -- absolute paths inside, must be per-machine
venv-ardupilot/

# Upstream projects with their own git history, checked out per machine
PX4-Autopilot/
ExpressLRS/
mavlink/
LINK_SHADE/
Qt/

# 웹 서비스 런타임 — 업로드 원본·캐시·서버로그·비밀은 커밋하지 않는다.
# (.ulg 는 위의 *.ulg 가 이미 덮는다. 벤더링한 Leaflet 은 커밋한다 — 그게 요점이다.)
web/data/
web/.env
web/*.log
web/tools/hosts.conf
