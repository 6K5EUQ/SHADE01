# 서버 venv 용. 버전을 고정한다 — pyulog 가 올라가면 파싱 결과가 달라질 수 있고,
# 그러면 캐시 지문과 실제 동작이 어긋난다.
# 실측 기준(2026-09-03): 이 조합으로 로그 67개 전량 파싱 성공.
pyulog==1.2.4
numpy==2.5.2

# 실시간 트래킹(web/live) 이 MAVLink 를 디코딩하는 데 쓴다. 로그 뷰어만 돌리는
# 서버에는 필요 없지만, 같은 venv 를 쓰는 운용 PC 에서는 이게 없으면
# `./qgc live on` 이 뜨지 않는다.
pymavlink==2.4.49
