# 비행 로그를 웹에 올리기 — 절차

비행이 끝나면 **한 줄**이다.

```bash
./shade01 sync
```

FC 에 있는데 랩서버에 없는 로그를 받아 **번호를 붙여** 올리고, **야외 비행이
아니면 랩서버에서 지운다.** 그 한 번에 일곱 단계가 다 돈다.

> 이 문서는 [`skills/flight-sync`](skills/flight-sync/SKILL.md) 의 정본이다.
> `./qgc sync` 는 같은 것을 가리키는 옛 이름이다 — 둘 다 동작한다.

---

## 무엇이 도는가

```
  [FC SD]  ──USB──  rim3  ──scp──  이 PC  ──rsync──  ku-labserver
     │                                                    │
     └─ 1 브리지 정지                          7 야외 판정 · 아니면 삭제
        2 목록 ↔ 랩서버 대조                     (판정이 서버 안에서 돈다)
        3 없는 것만 받기                              │
        4 회수                                shade01.bewe.co.kr
        5 번호 라벨링                          localhost:4400
        6 업로드
```

| # | 단계 | 하는 일 |
|---|---|---|
| 1 | **브리지 정지** | 유닛이 `--user` 인지 시스템인지 알아서 가른다. **어떻게 끝나든 복구된다** |
| 2 | **대조** | FC 의 로그 전부 ↔ 랩서버 보유분 + 삭제 기록. **집합 차이**다 |
| 3 | **받기** | 없는 것만. 1MB 미만은 받지도 않는다 |
| 4 | **회수** | rim3 → 이 PC 의 `logs/` |
| 5 | **번호 라벨링** | FC 에 엔트리 번호를 물어 `log_<번호>_<KST>.ulg` 로 |
| 6 | **업로드** | 랩서버 `~/shade01-data/logs/` |
| 7 | **판정·삭제** | **랩서버 안에서** 야외 비행인지 보고, 아니면 지우고 기록에 남긴다 |

🔴 **브리지는 3·4단계가 끝나면 바로 되살린다.** 5단계부터는 FC 와 무관하므로
링크를 더 붙들 이유가 없다. `trap` 이 `EXIT INT TERM HUP PIPE` 를 잡아
Ctrl-C 로 끊거나 중간에 실패해도 복구가 돈다.

---

## 쓰는 법

```bash
./shade01 sync                 # 이게 전부다
./shade01 sync --dry           # 무엇을 할지 보여주기만
./shade01 sync --verify        # 5회씩 받아 바이트 다수결로 복원 (느리지만 정확)
./shade01 sync --verify=7      # 더 확실하게
./shade01 sync --min-size=0.5  # 짧은 비행을 놓친 것 같을 때
./shade01 sync --keep-ground   # 지상 시험도 남긴다
./shade01 sync --all           # 삭제 기록까지 무시하고 FC 전체를 다시 본다
```

### 출력 읽는 법

```
== 2/7 FC 목록 ↔ 랩서버 대조 ==
  FC 폴더 10개
  FC 로그 247개
  랩서버 보유 118개 · 판정 후 지운 것 0개
  랩서버에 없는 것 126개
  받을 것 35개 / 1.0MB 미만 91개 건너뜀 (그중 최대 0.94MB)
  ⚠️ 건너뛴 것 중 0.94MB 가 문턱에 가깝다 — 놓친 것 같으면 --min-size=0.5
```

| 줄 | 뜻 |
|---|---|
| `랩서버에 없는 것 N개` | 대조 결과. 0 이면 새 비행이 없다 |
| `⚠️ … 문턱에 가깝다` | **읽어라.** 짧은 비행을 놓쳤을 수 있는 유일한 경로다 |
| `→ log_246_…` | 번호가 붙었다 |
| `− … ground` | 야외 비행이 아니라 랩서버에서 지웠다 |
| `✅ 브리지 복구` | **이 줄이 없으면 QGC 링크가 끊긴 채다** |

---

## 🔴 "마지막 이후" 가 아니라 "없는 것"

증분을 **시각 cutoff** 로 잡으면 중간의 구멍을 못 본다 — 업로드가 한 번
실패했거나 누가 손으로 지운 로그는 영영 안 올라간다. 그래서 **집합 차이**로
바꿨다: FC 가 가진 것에서 랩서버가 가진 것과 삭제 기록을 뺀다.

이름이 세 형식이라 이름만으로는 못 맞춘다:

| 형식 | 시간대 | 예 |
|---|---|---|
| `YYYY-MM-DD_HH_MM_SS.ulg` | **UTC** — FC 에서 그대로, 로그 **시작** 시각 | `2026-09-05_09_24_04.ulg` |
| `log_<n>_YYYY-M-D-H-M-S.ulg` | **KST** — 번호가 붙은 뒤, 로그 **종료** 시각 | `log_244_2026-9-5-18-31-28.ulg` |
| `RECOVERED*` 등 | — | 손으로 만든 것 |

[`since.py`](tools/flightsync/since.py) 가 전부 **UTC** 로 맞춰 비교한다.
시작·종료 차이를 흡수하려고 창(`_MATCH_WINDOW_S`)을 둔다.

⚠️ **창을 넓히면 이웃 로그를 같은 것으로 오인해 새 비행을 안 받는다.**
좁히면 이미 있는 것을 또 받는다 — 그쪽이 덜 나쁘므로 그렇게 기울여 뒀다.

## 🔴 지운 번호는 다시 채우지 않는다

7단계가 지운 로그는 랩서버의 **`~/shade01-data/pruned.txt`** 에 남는다.

```
2026-09-05_04_14_10.ulg	abort	2026-09-06T09:12:33+09:00
```

이게 없으면 다음 sync 가 "랩서버에 없네" 하고 **다시 받고**, 판정이 **다시
지운다** — 무한 반복이다.

**번호는 그대로 둔다.** 비행 번호는 FC 가 매기는 일련번호라 우리가 채울 수
있는 것도 아니고, `log_243` 다음이 `log_246` 인 편이 정직하다 — 그 사이가
지상 시험이었다는 뜻이다.

`--all` 은 이 기록을 무시한다. 판정 기준을 바꾼 뒤 다시 훑을 때만 쓴다.

---

## 왜 빠른가 — 받는 개수를 줄이는 것이 유일하게 듣는다

MAVFTP 전송 자체는 이미 빠르다. **느린 것은 파일마다 새로 연결하는 비용이다.**

2026-09-05 실측 49개 79MB, **562초**:

| | |
|---|---|
| 전송 속도 | **0.50 MB/s** (22.41MB→45초, 17.36MB→35초 회귀) |
| 파일당 = | `0.6초 + 1.98초/MB` |
| 전송 합계 | 185초 |
| **재연결** | **377초 = 파일당 7.7초 × 49회** |

**총시간의 72% 가 재연결이다.** 그리고 줄일 방법이 없다:

🔴 **FC 는 한 연결에 파일 하나만 내준다.** 두 번째부터
`OpenFileRO failed, no sessions available` 로 0바이트가 되고, 세션 반납
(`ResetSessions`)으로도 안 풀린다 — 실측 확인.
`fcfetch.py` 가 파일마다 재연결하는 이유가 그것이다.
**시리얼 포트도 하나뿐**이라 프로세스를 늘려도 못 붙는다.

→ **병렬은 불가능하다. 받는 개수를 줄이는 것만 남는다.**

### 크기 게이트

2026-09-05 실측 49개의 분포:

| 종류 | 크기 | 개수 |
|---|---|---|
| **실비행·호버** | **1.17 ~ 22.41 MB** | 8 |
| 지상·즉시disarm | 0.19 ~ 0.95 MB | 41 |

경계에 가장 가까운 것은 **지상 로그 0.95MB**, 호버 최소는 **1.17MB**.
그래서 문턱을 **1.0MB** 로 잡았다.

| | 전체 받기 | 1MB 게이트 |
|---|---|---|
| 받는 개수 | 49 | **10** |
| 재연결 | 377초 | 77초 |
| 전송 | 185초 | 131초 |
| **합계** | **562초** | **~208초** |

**2.7배 빠르다.** 그리고 실비행 3편·호버 5편을 하나도 안 놓친다.

⚠️ **놓칠 수 있는 유일한 경로다.** 그래서 건너뛴 것 중 **가장 큰 크기를 항상
출력**하고, 문턱의 80% 를 넘으면 경고한다:

```
받을 것 10개 / 1.0MB 미만 39개 건너뜀 (그중 최대 0.95MB)
⚠️ 건너뛴 것 중 0.95MB 가 문턱에 가깝다. 놓친 것 같으면: ./qgc sync --min-size=0.5
```

---

## 로그 번호가 붙는다

받은 파일은 `log_<번호>_<KST시각>.ulg` 로 개명된다. 번호는 **이 기체의 비행
일련번호**다 — QGC 가 붙이는 그 번호이고, 지금 246번까지 왔다.

```
2026-09-05_09_34_04.ulg  →  log_246_2026-9-5-18-37-18.ulg
```

MAVFTP 는 SD 디렉토리를 훑을 뿐이라 번호가 안 온다. 번호는
`LOG_REQUEST_LIST` 라는 다른 프로토콜에 있고, [`fclist.py`](tools/qgclog/fclist.py)
가 그것을 물어 온다 (**목록 조회다 — FC 상태를 바꾸지 않는다**).
짝짓기는 **크기 우선**이다. 시각은 ±2초 어긋나도 크기는 안 어긋난다.

🔴 **이번에 받은 파일만 개명한다** ([`number_new.py`](tools/flightsync/number_new.py)).
디렉토리 전체를 개명하면 두 가지가 깨진다:

1. **서버 이름과 어긋난다** — 이름으로 대조하는 증분·정리가 헛돈다
2. **중복쌍에서 충돌한다** — 같은 비행이 두 이름으로 있으면(`log_108` 사례)
   `fclist` 가 이름 충돌을 발견하고 **전체를 취소한다** (실측)

새로 받은 파일은 아직 아무 데도 안 올라갔으므로 어긋날 상대가 없다.

⚠️ **크기까지 일치할 때만 붙인다.** 시각만 맞으면 그대로 둔다 —
틀린 번호는 없는 번호보다 나쁘다.

⚠️ 이름이 바뀌어도 증분은 안 깨진다. `since.py` 가 두 형식의 시간대 차이를
안다 (FC 이름=UTC, QGC 이름=KST).

## 실측 — 2026-09-06 전체 실행

```
FC 폴더 10개 · FC 로그 247개
랩서버 보유 118개 · 판정 후 지운 것 0개
랩서버에 없는 것 126개
받을 것 35개 / 1.0MB 미만 91개 건너뜀 (그중 최대 0.94MB)
  → 34개 받음, 1개 실패(pymavlink 간헐 버그 — 그 뒤 재시도 3회를 넣었다)
  → 30개에 번호를 붙였다 (log_6 … log_171)
  → 34개 올렸다 (95.6MB)
  → 야외 비행 58개 유지 / 94개 삭제
```

다시 돌리면:

```
랩서버 보유 58개 · 판정 후 지운 것 94개
랩서버에 없는 것 17개
받을 것 0개 / 1.0MB 미만 17개 건너뜀
```

**멱등이다** — 지운 94개를 다시 받지 않는다. 남은 17개는 전부 1MB 미만이다.

## 7단계 — 판정은 랩서버 안에서 돈다

업로드가 끝나면 **랩서버가 자기 로그를 전부 판정한다.** 야외 비행(`flight`·
`hover`)이 아니면 지우고 `pruned.txt` 에 남긴다.

```bash
ssh ku@ku-labserver \
  '~/shade01-venv/bin/python ~/SHADE01/web/tools/uploadable.py ~/shade01-data/logs/*.ulg'
```

🔴 **왜 이 PC 가 아니라 랩서버인가.** 판정하려면 파일을 읽어야 한다. 이 PC 에서
돌리면 **사본이 없는 로그는 판정할 수 없다** — 다른 PC 가 올린 것이 그렇다
(실제로 gram 에만 있던 20개가 9/6 에 발견됐다). 서버는 자기가 가진 것을 전부
읽을 수 있다.

랩서버에는 `~/shade01-venv` 에 `pyulog`·`numpy` 가 있고, `~/SHADE01` 리포도
있다. sync 가 돌기 전에 `git pull` 을 한 번 걸어 판정 코드를 맞춘다.

## 무엇이 올라가는가

**`flight` 와 `hover` 만.** 배지는 [`web/extract.py`](web/extract.py) 의
`classify()` 가 정한다 — 목록 배지를 만드는 그 함수다.

| 배지 | 기준 | 올라가나 |
|---|---|---|
| `flight` | 속도 ≥ 3 m/s **또는** 고도 ≥ 10 m | ✅ |
| `hover` | 그 밖에 6초 넘게 뜬 것 | ✅ |
| `ground` | 고도 < 0.5 m, 속도 ≤ 0.5 m/s | ❌ |
| `abort` | arm 후 **6초 이하** | ❌ |
| `noarm` | arm 안 됨 | ❌ |
| `unknown` | 고도·속도를 못 읽음 | ❌ |
| `indoor` | GPS 가 **3D fix 를 한 번도 못 잡음** | ❌ |
| `unreadable` | 서버가 렌더하지 못함 | ❌ |

지상 시험을 올리려면 `--all-badges`. 안 올려도 **`.ulg` 는 이 PC 와 FC 에
그대로 남아 있다** — `./qgc log list` 로 언제든 본다.

### 🔴 판정은 서버가 실제로 도는 경로를 그대로 태운다

`summarize()` 만 보고 통과시키면 안 된다. 서버는 `extract.py full` 을 돌리고,
**요약이 멀쩡해도 `build_track()` 에서 터지는 로그가 있다.**

실측(2026-09-05): `SP` 미정의로 6개가 목록에서 회색 행이 됐는데,
`extract.py row` 로는 **전부 정상으로 보였다.** 그래서
[`uploadable.py`](web/tools/uploadable.py) 는 `armed_window` → `flight_key` →
`decoded_points` → `build_track` 까지 같은 순서로 부른다.

### 실내 판정에 함정이 둘 있다 — 둘 다 실측으로 찾았다

**1. GPS 토픽 이름이 펌웨어마다 다르다.** `sensor_gps` 만 보면 9/2 의
**실제 야외 비행**(`log_182`·`log_186`)이 "GPS 없음"으로 잡힌다 — 그쪽은
`vehicle_gps_position` 을 쓴다. 이대로 지웠으면 실비행을 날릴 뻔했다.

**2. `fix_type` 에도 깨진 바이트가 온다.** `log_182` 는 1584샘플이 `4`(RTK)인데
한 샘플만 **215** 였다. 0~8 밖의 값을 버리고 최대를 본다.

⚠️ **GPS 토픽이 아예 없는 것은 실내로 치지 않는다.** 너무 짧아 한 번도 발행되지
않았을 수 있다 (실측: 9/5 야외 세션의 1.5초 로그).

### fix 등급

| fix | 뜻 | 오차 |
|---|---|---|
| 0-1 | 측위 없음 | — |
| 2 | 2D | 수십 m |
| **3** | **3D (보통 GPS)** | **1~20 m** |
| **4** | **RTK Fixed** | **cm** |
| 5 | RTK Float | 수십 cm |

⚠️ **5(Float)가 4(Fixed)보다 나쁘다.** 숫자 순서와 정확도가 4에서 꺾인다.
그래서 코드는 `>= 3` 으로만 판정하고 4·5 를 비교하지 않는다.

---

## 설정

주소는 [`web/tools/hosts.conf`](web/tools/hosts.conf.example) 에 있다
(**gitignore 됨** — 리포가 public 이다).

```bash
FC_HOST="rim3@rim3"                            # FC 가 USB 로 붙어 있는 PC
FC_VENV="~/.venv-mav/bin/python"               # 그 PC 의 pymavlink
FC_LOGDIR="~/SHADE01/logs"
SERVER="ku@ku-labserver"                       # = ku@100.86.239.31
SERVER_LOGS="/home/ku/shade01-data/logs"       # 정본 보관소
SERVER_VENV="~/shade01-venv/bin/python"        # 랩서버의 pyulog (판정용)
SERVER_REPO="~/SHADE01"
```

**FC 를 다른 PC 로 옮기면 `FC_HOST` 한 줄만 바꾼다.** 2026-09-04 에
raspb1 → rim3 로 옮겼고, 브리지 유닛 이름·스코프는 스크립트가 알아서 가른다
(`rim3` = `--user shade-bridge`, `raspb1` = 시스템 `mavlink-bridge`).

### 새 PC 에서 한 번만

FC 쪽 PC 에 pymavlink:

```bash
ssh rim3@rim3
python3 -m venv --without-pip ~/.venv-mav
curl -sS https://bootstrap.pypa.io/get-pip.py -o /tmp/get-pip.py
~/.venv-mav/bin/python /tmp/get-pip.py -q
~/.venv-mav/bin/pip install pymavlink pyserial
rm -f /tmp/get-pip.py
```

랩서버에 판정용 인터프리터 + 리포 (이미 있다):

```bash
ssh ku@ku-labserver '~/shade01-venv/bin/python -c "import pyulog,numpy"'
ssh ku@ku-labserver 'cd ~/SHADE01 && git pull'
```

## 장애 대응

| 증상 | 원인 | 대응 |
|---|---|---|
| `FC 폴더 목록을 못 읽었다 (3회 시도)` | 브리지가 아직 포트를 쥐고 있다 | `ssh $FC_HOST 'fuser -v /dev/ttyACM0'` |
| `TypeError: 'NoneType' …` 가 보였다 | pymavlink 2.4.49 간헐 버그 | **재시도가 3회 걸려 있다.** 그래도 실패하면 USB 재연결 |
| 받기 실패가 여럿 | USB 케이블 / FC 재부팅 | 다시 돌리면 받은 것은 건너뛴다 |
| `랩서버에 없는 것 0개` | 새 비행이 없다 | 정상. 판정은 그래도 돈다 |
| `판정을 못 돌렸다` | 랩서버의 venv·리포 문제 | `ssh $SERVER '~/shade01-venv/bin/python -c "import pyulog"'` |
| 서버가 502 | 캐시 굽는 중 | 15회×4초 기다린다. 그래도면 `journalctl -u lab-shade01` |
| `✗ 브리지 복구 실패` | 🔴 **QGC 링크가 끊긴 채다** | `ssh $FC_HOST 'systemctl --user start shade-bridge.service'` |

**되풀이해서 돌려도 안전하다.** 받은 것·올린 것·지운 것 전부 건너뛴다.

⚠️ **출력을 `| head` 로 자르지 마라.** SIGPIPE 로 죽는다. 복구 트랩이 `PIPE`
까지 잡도록 고쳐 뒀지만(실측으로 물렸다) 뒷부분을 못 보는 것은 그대로다.
길면 파일로 받아라.

## 🔴 FC 의 SD 읽기가 매번 다른 데이터를 낸다 (2026-09-06 발견)

**같은 파일을 다시 받으면 크기는 같고 내용이 다르다.** 이 도구를 만들며
`09_17_20.ulg`(1.17MB)를 네 번 받았더니 md5 가 전부 달랐다.

전송 문제가 아니다. **FC 자신이 계산한 CRC 도 매번 다르다:**

```
한 연결에서 5회   0xc6d7855d  0x5015a2b7  0xf879358a  0xb07bcdd5  0x6b2c389d
새 연결로 3회     0xb07bcdd5  0xcdfd85d9  0x8356a46e
```

정지된 파일이다. FC 가 SD 를 읽을 때마다 다른 바이트를 낸다.

### 파이프라인 실측 — 7개 재다운로드, 7개 전부 손상 (2026-09-06)

`./qgc sync` 를 실제로 태워 검증했다. 이미 받아 둔 7개를 지우고 다시 받으니:

| 파일 | 이전 md5 | 새로 받음 | 크기 |
|---|---|---|---|
| 09_03_07 | f2977a8e | d7d1bb17 | 같음 |
| 09_04_23 | 2e63cb01 | 40c0540e | 같음 |
| 09_05_46 | 23afb970 | 4559c017 | 같음 |
| 09_08_30 | 681da299 | 5f0dbb4b | 같음 |
| 09_10_57 | d5abe0c6 | f151b3e4 | 같음 |
| 09_11_24 | 749af0f0 | 78ae8be0 | 같음 |
| 09_17_20 | 7d1b4ea5 | 7b3ec113 | 같음 |

**7/7 손상.** 그리고 판정까지 바뀌었다 — `09_08_30` 은 원래 `ground` 인데
새로 받은 것은 **`unreadable`** 이 됐다. 손상이 포맷 정의 구간에 떨어진 것이다.

⚠️ **`rsync --ignore-existing` 이 서버 정본을 지켜 줬다.** 새로 받은 손상본이
서버를 덮어쓰지 않았다. 이 플래그를 빼면 재실행할 때마다 정본이 나빠진다.

### 오류율은 읽은 바이트에 비례한다

| 파일 | CRC 4회 중 고유값 |
|---|---|
| 0.19 MB | 2 |
| 0.72 MB | 3 |
| **1.17 MB** | **4 (전부 다름)** |

역산하면 **바이트당 약 1.5e-6**, 즉 **700KB 마다 1바이트꼴**이다.
그러면 22MB 로그가 깨끗할 확률은 사실상 **0** 이다.

### 이것이 설명하는 것들

`PROCEDURE.md` 가 "SD 카드 손상" 으로 적어 온 현상 대부분이 여기서 나온다:

- **구독 섹션 유실** — `A` 블록의 한 바이트가 깨져 pyulog 가 토픽을 못 붙인다
- **포맷 정의 유실** — 정의 구간 바이트가 `A`(0x41)·`B`·`F` 로 오독된다
- **가짜 FLAGS_BITS** — 난수 바이트가 `B` 로 읽힌다
- **깨진 float 한 샘플** — 고도 `5.03e14 m`, 쿼터니언 norm `4.25e37`
- **`fix_type` 이 215** — `log_182` 의 1584샘플 중 하나

전부 **한 바이트 오류**의 결과다. `qgclog._repair()` 와 `_sane()` 필터가
지금까지 이걸 덮어 왔던 것이다.

### 파서는 결정적이다 — 파일이 다른 것이다

먼저 이걸 갈랐다. 디스크의 **같은 파일**을 5회 분석하면 출력이 바이트 단위로
같다. 로그 6개(복구가 걸리는 것·깨진 바이트가 있는 `log_182` 포함)를 3회씩
돌려도 전부 고유 출력 1개다. **파싱은 흔들리지 않는다.**

md5 도 결정적이므로, 다운로드마다 md5 가 다르다는 것은 **파일이 실제로 다르다**는
뜻이다.

### 손상은 512바이트 섹터 단위다

같은 파일을 6회 받아 서로 비교했다:

| 비교 | 다른 바이트 | 다른 섹터 |
|---|---|---|
| dl1 vs dl2 | 334 | 12 |
| dl1 vs dl3 | 194 | 7 |
| dl1 vs dl4 | 184 | 7 |
| dl1 vs dl5 | 410 | 14 |
| dl1 vs dl6 | 308 | 11 |

차이는 **512의 배수 오프셋에서 시작**한다 (50176, 189440, 574464, 587264 …) —
SD 섹터 크기다. 한 번 읽을 때 2278섹터 중 **7~14개(약 1.4%)** 가 어긋나고,
**어긋나는 섹터가 매번 다르다.**

### ✅ 다수결로 원본을 복원할 수 있다

섹터가 매번 다르게 깨지므로, 여러 번 받아 위치별로 **가장 많이 나온 바이트**를
고르면 원본이 돌아온다. [`tools/qgclog/fcvote.py`](tools/qgclog/fcvote.py) 가 한다.

```bash
./qgc sync --verify          # 5회씩 받아 다수결
./qgc sync --verify=7        # 더 확실하게
```

**실측 — 서로 다른 4회 시행이 바이트 단위로 같은 결과를 냈다:**

| 시행 | 표본 | 결과 md5 | px4crc |
|---|---|---|---|
| 1차 | 6회 | `53dc1a53…` | `0xb07bcdd5` |
| 2차 | 5회 | `53dc1a53…` | `0xb07bcdd5` |
| 3차 | 5회 | `53dc1a53…` | `0xb07bcdd5` |
| 4차 (`--verify`) | 5회 | `53dc1a53…` | `0xb07bcdd5` |

- 과반 미달 바이트 **0개** (매번)
- **leave-one-out 안정** — 표본 하나를 빼도 결과가 같다 (6/6)
- 복원본의 CRC `0xb07bcdd5` 는 **FC 자신이 낸 CRC 값 중 하나다** — 독립 교차검증

**그리고 손상은 실제로 수치를 바꾼다.** 같은 비행을 서버 사본과 복원본으로
읽으면:

| | 비행시간 | 최고고도 | 소모 |
|---|---|---|---|
| 서버 사본 (손상) | 19초 | 1.1 m | 66 mAh |
| **복원본 (진짜)** | **20초** | **1.2 m** | **68 mAh** |

한 판본(`dl1`)은 arm 을 아예 못 찾아 **`noarm`** 으로 떨어지기도 했다.

⚠️ **비용은 N 배다.** 1.17MB 가 회당 6초라 5회면 30초, 22MB 는 회당 45초라
5회면 4분이다. 그래서 기본은 꺼 두고, 정확도가 필요할 때 켠다.

### 그래서 단순 CRC 검증은 넣지 않았다

**기준값 자체가 흔들리므로 단순 비교는 무의미하다.** FC CRC 도 읽을 때마다 다르니
"맞을 때까지 재시도" 는 큰 파일에서 수렴하지 않는다 (4회 전부 불일치 실측).
**대신 다수결을 쓴다** — 위 절을 보라.

### 무엇을 해야 하나

1. 🔴 **SD 카드를 교체하고 위 시험을 다시 돌려라.** CRC 가 안정되면 카드 불량이었다.
   가장 싸고 빠른 가설 검증이다
2. 🔴 새 카드에서도 흔들리면 **FC 의 SD 인터페이스** 문제다 (납땜·슬롯)
3. ⚡ **정확한 로그가 필요하면 SD 를 빼서 PC 카드리더로 읽어라.**
   FC 를 거치지 않으므로 이 문제를 통째로 우회한다
4. ⚠️ **기존 분석 수치를 절대적으로 믿지 마라.** 대부분은 필터가 걸러 낸 뒤라
   쓸 만하지만, 한 바이트 오류가 조용히 값을 바꿨을 가능성은 남는다

### 시험을 다시 돌리는 법

같은 파일의 CRC 를 여러 번 물어 값이 흔들리는지 본다:

```bash
ssh rim3@rim3 'systemctl --user stop shade-bridge.service'
scp tools/qgclog/fccrc.py tools/qgclog/fcfetch.py rim3@rim3:/tmp/
ssh rim3@rim3 'cd /tmp && ~/.venv-mav/bin/python fccrc.py /fs/microsd/log/2026-09-05/09_17_20.ulg 5'
ssh rim3@rim3 'systemctl --user start shade-bridge.service'
```

값이 전부 같으면 `✅ 안정`, 흔들리면 `🔴 불안정` 이 나온다. 읽기 전용이다.


---

## 한 번 더 — 지켜야 할 것

- 🔴 **`.ulg` 는 git 에 안 들어간다.** 수치를 [`flights/`](flights/) 에 적는 것이
  유일한 영구 기록이다. 서버가 원본 보관소다
- 🔴 **`logs/` 는 평면으로 쌓는다.** `qgclog._repair()` 가 **같은 디렉토리의**
  형제 로그를 기증자로 쓴다. 날짜 하위폴더를 만들지 마라
- 🔴 **`pyulog` 를 직접 부르지 마라.** 잘린 메시지에서 조용히 멈춘다
- ⚠️ **FC 파일명은 UTC.** 저장할 때 `<UTC날짜>_<시각>.ulg` 로 바뀐다. KST = UTC+9
